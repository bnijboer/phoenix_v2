<template>
    <slot />
</template>

<script setup>
    const props = defineProps({
        'rules': {
            type: Object,
        }
    });

    onMou

    // props.rules.forEach(rule => console.log(rule(form.body)));
</script>
